// Server URL is now read directly from .env via preload.js
// localStorage is no longer used for server URL configuration
console.log('🌐 Server URL is managed via .env file:', window.SERVER_URL_ENV);
